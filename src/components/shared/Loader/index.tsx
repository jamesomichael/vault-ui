const Loader = () => {
	return (
		<div className="flex items-center justify-center w-full h-full">
			<div
				className={`w-10 aspect-square border-2 border-t-transparent border-blue-400 rounded-full animate-spin`}
			></div>
		</div>
	);
};

export default Loader;
